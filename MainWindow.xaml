<Window x:Class="RadioPlayer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Online Radio"
        Height="350"
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#111827">

    <Grid>

        <Border
            Width="420"
            Height="276"
            CornerRadius="20"
            Background="#1F2937"
            VerticalAlignment="Center"
            HorizontalAlignment="Center">

            <StackPanel Margin="30">

                <TextBlock
                    Text="🎵 ONLINE RADIO"
                    FontSize="28"
                    FontWeight="Bold"
                    Foreground="White"
                    HorizontalAlignment="Center"
                    Margin="0,0,0,25"/>

                <TextBlock
                    Text="Radio URL"
                    Foreground="#9CA3AF"
                    FontSize="14"
                    Margin="0,0,0,8"/>

                <Border
                    CornerRadius="10"
                    Background="#374151"
                    Padding="10">

                    <TextBox
                        x:Name="UrlTextBox"
                        BorderThickness="0"
                        Background="Transparent"
                        Foreground="White"
                        FontSize="14"
                        Text="http://ice1.somafm.com/groovesalad-128-mp3" TextChanged="UrlTextBox_TextChanged"/>
                </Border>

                <StackPanel
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,35,0,0">

                    <Button
                        Width="120"
                        Height="45"
                        Margin="10"
                        Content="▶ START"
                        FontSize="16"
                        FontWeight="Bold"
                        Background="#10B981"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="StartButton_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="12">
                                                <ContentPresenter
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                    </Button>

                    <Button
                        Width="120"
                        Height="45"
                        Margin="10"
                        Content="■ STOP"
                        FontSize="16"
                        FontWeight="Bold"
                        Background="#EF4444"
                        Foreground="White"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="StopButton_Click">

                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border
                                                Background="{TemplateBinding Background}"
                                                CornerRadius="12">
                                                <ContentPresenter
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </Button.Style>

                    </Button>

                </StackPanel>

            </StackPanel>

        </Border>

    </Grid>
</Window>
        </StackPanel>
    </Grid>
</Window>
